{
  "name": "qr-browser",
  "version": "1.0.0",
  "description": "QR Code Scanner & Browser",
  "scripts": {
    "build": "echo No build step needed",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
